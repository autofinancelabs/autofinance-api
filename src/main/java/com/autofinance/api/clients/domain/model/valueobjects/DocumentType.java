package com.autofinance.api.clients.domain.model.valueobjects;

/** Type of identity document (Peru): national ID, foreigner card, or passport. */
public enum DocumentType {
    DNI,
    CE,
    PASAPORTE
}
