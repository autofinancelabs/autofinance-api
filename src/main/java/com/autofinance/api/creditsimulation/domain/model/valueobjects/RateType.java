package com.autofinance.api.creditsimulation.domain.model.valueobjects;

/** Nature of an interest rate: nominal (needs capitalization) or effective. */
public enum RateType {
    NOMINAL,
    EFFECTIVE
}
